# マイルストーン

- 文書番号: GV-MS-001
- 版: 1.0
- 作成日: 2026-08-15

**この文書は「どこまで来たか」だけを述べる。** 何を作るかは要件定義書、どう作るかは基本設計書と詳細設計書、
検証の手順は [04-handover-verification.md](./04-handover-verification.md)、検証の結果は詳細設計書 §13 が正である。
**食い違ったら、それぞれの正本が正しい。**

---

## 現在地

**フェーズ 2 まで完了。実機で動いている。**

| 指標 | 値 |
|---|---|
| テスト | **1008 件・失敗 0・警告 0** |
| 外部依存パッケージ | **0** |
| 配布サイズ | 0.9 MB（フェーズ 1 実測。`.app` 化後は未計測） |
| ネットワーク通信 | **無し**（NFR-V1） |
| 音声のディスク書き出し | **無し**（NFR-V2） |

```
main  a418ef6
 ├ PR #1  フェーズ1（CLI で一気通貫）
 ├ PR #2  フェーズ2（HUD・設定・履歴・Undo と、整形を挿入の前提から外す設計変更）
 └ PR #3  実機で見つかった4件の修正と、HUD をダイナミックアイランド風へ
```

---

## フェーズ 1（完了 / PR #1）

**目標**: 押して喋れば、カーソル位置にテキストが入る。

CLI で端から端まで通した。`SpeechAnalyzer` + `FoundationModels` のみ、外部 LLM も API も使わない。

**実測で覆った設計時の想定（7 件）**——`contextualStrings` が効かない / `prewarm()` では温まらない /
日本語では `DictationTranscriber` が優位 / `.app` バンドルは（当時は）不要 / ほか。

**実機で見つかった欠陥**: 右 Option を持たない配列がある（L-8）/ 末尾 38 字の欠落（V-12）/
クリップボードの復元待ち不足で違う内容が貼られる / モデルが天気を捏造した。

---

## フェーズ 2（完了 / PR #2・#3）

**目標**: フェーズ 1 で「未実装」だった必須要件をすべて満たす。

### 実装したもの

| 要件 | 内容 |
|---|---|
| FR-2 / FR-3 | **notch HUD**（自前の `NSPanel`。切り欠きを覆う 1 枚の島） |
| FR-7 | **Undo**（AX の範囲上書き。⌫ 送出は採らない） |
| FR-9 | **履歴 UI**（一覧・再挿入・コピー・削除） |
| FR-10 | **権限案内**（`.app` としての初回起動） |
| FR-11 | **設定 UI**（打鍵の捕獲・辞書の編集を含む） |
| — | **`Ghost Voice.app`**（`LSUIElement` 常駐 / Apple Development 証明書署名） |

### 要件そのものを変えた

**3 つの必須要件が 1 秒という同じ予算を奪い合って両立していなかった。**

- **NFR-P6 を分割**: P6a（テキストが出るまで 1 秒。全発話長）/ P6b（整形の反映。**推定値**）
- **FR-5**: 「整形完了を待って挿入」→ **生テキストを先に挿入し、整形できたら条件付きで差し替える**
- **NFR-V3 に最小例外**（**利用者の明示的な承認を得た**）: 自分が書いた範囲だけを読み戻し、比較のみ、保持しない、
  不一致の内容は見ない。**4 条件すべてを検査で固定している**

### 実機で見つかった欠陥（4 件。**すべて代役の検査は緑のまま実機で落ちた**）

1. **`.app` が SIGTERM で終了しない** — シグナルの受け口がメインキューにあり、`.terminateLater` の入れ子の
   ランループで自己デッドロック。**HUD が出なかったのも同じ原因**（`@MainActor` が全部死んだ。
   `CGEventTap` は CFRunLoop 側なので PTT だけ動き続けた）
2. **終了待ちが画面に出ない** — 正しく待っているのに壊れて見える。**猶予は変えずに表示を足しただけで、
   同じ手順の結果が「発話が消える」から「挿入される」に変わった**
3. **猶予切れで打ち切った発話がどこにも残らない（A）** — `ESC` は履歴へ残すのに打ち切りだけ穴が空いていた
4. **整形が実運用でほぼ効かない** — 原因は **Apple Intelligence のデーモンが発話の間に冷えること**。
   「18.5 ms/字」は誤りで、正しくは `365 ms + 5.26 ms/字`

---

## 実機で確かめたもの

| 項目 | 結果 |
|---|---|
| **V-19** `CGEventTap` が `NSApp.run()` 下で届くか | **成立**（不成立なら器の作り直しだった） |
| **V-16** 許可が再ビルドを跨ぐか | **成立**（証明書署名を選んだ判断の裏づけ） |
| **V-34** 発話の途中の終了で発話を失わないか | **成立**（上記 2 の修正後） |
| **FR-2 / FR-3** HUD | **成立** |
| AX 直接挿入 | TextEdit で成立 |
| 権限の付け直し | 4 つとも成立 |

## 実機で確かめていないもの

**詳細設計書 §13 の 44 項目のうち、29 項目が未実施である。** 手順は [04-handover-verification.md](./04-handover-verification.md)。

**とくに重いもの:**

- **段 5（V-23〜V-27）**: 差し替えが「消えただけで戻らない」経路が実在するか。
  **実在するなら差し替えを既定オフにする判断が要る**（R-9）
- **段 4（V-3）**: メール / Slack / Notion / Xcode での挿入経路
- **V-45**: 暖機の効果を `SpeechAnalyzer` と同時に動かして測る
- **V-1**: 肉声での認識精度（`transcriberKind` の既定を決める）

---

## 正直に残しておくこと

**動いてはいるが、期待どおりではない部分がある。**

- **整形が実運用で意味のある変化を生んだ回数は 0 回**（利用者の履歴 10 件を実測）。
  冷却の修正で**間に合う発話は増えたが、間に合った結果が有用かは別問題**である
- **利用者の実運用では 10 件中 9 件が Pasteboard 経路**。
  **FR-5(a) の差し替えも FR-7 の Undo も、その経路では効かない。**
  §2.8.6 の「(a) が既定」は、少なくともこの利用者には当てはまらない
- **削ることで意味が変わる逸脱**（否定の脱落）は文字レベルの検査では原理的に捕まえられない。
  **「通ること」を検査で固定**して、守っているつもりを作らないようにしてある。安全網は Undo と履歴の `rawText`
- **NFR-P6b（整形の反映 2 秒）は推定値**である
- **断続的に落ちる検査が残っている。** 機序 2 つ（`SessionMirror` の購読レース / 実 Apple Intelligence の冷え）は
  特定して直したが、**クリーンビルド直後に 1 件落ちた回があり、原因は分かっていない**（2026-08-15）

---

## フェーズ 3 の候補（優先順）

**順序は「発話を失うか」→「機能するか」→「使いやすいか」で決めてある。**

| # | 内容 | なぜ |
|---|---|---|
| 1 | **段 5 の実施と、結果に応じた既定値の決定** | **差し替えが「消えただけ」になるなら、発話を失う。既定オフの判断が要る** |
| 2 | **整形が価値を出しているかの判断** | **0 回だった。** 出していないなら FR-5 そのものを見直す |
| 3 | **Pasteboard 経路が主用途である事実への対応** | 差し替えも Undo も効かない。**設計の前提が実運用と食い違っている** |
| 4 | 断続的に落ちる検査の 3 つ目の機序 | **落ちるゲートは読み飛ばされ、無いより悪い**（開発サイクル §5） |
| 5 | ステータス項目が切り欠きの裏に隠れる | **`LSUIElement` なので唯一の入口**。利用者側の事情で消えうる |
| 6 | 危険な設定への防御 | **1 文字の常用漢字を辞書へ登録すると、その文字を含む全発話に影響する**（実際に起きた） |
| 7 | 持ち越しの C 級 2 件 | `.refusedSecureInput` が `clearUndoTarget()` を飛ばす / ESC の宛先の曖昧さ |
| 8 | 残り 29 項目の検証 | §13 |

---

## この製品で繰り返し出た欠陥の型

**「成功と記録されるのに、そこにテキストが無い（あるいは違うテキストがある）」。**

フェーズ 1 で 10 件以上、フェーズ 2 でさらに出た。最大のものは
**「実装したと記録されているのに、製品では一度も動かない」**（本番の組み立てが差し替え器を通っていなかった）。

**そして実機で出た 4 件は、すべて代役の検査が緑のままだった。**
`NSApplication` と実際のメインキュー、実際の TCC、実際のモデルの温度——**代役では作れない条件がある。**
